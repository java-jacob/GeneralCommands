Windows Docker Image Save in anotehr location:
----------------------------------------------

wsl --list -v
wsl --export docker-desktop-data "H:\docker-desktop-data.tar"
wsl --unregister docker-desktop-data
wsl --import docker-desktop-data  "H:\Docker" "H:\docker-desktop-data.tar" --version 2

Delete the Tar File: 

-- Fist hello docker from local. 
docker build -t hello-docker .
docker images
docker run hello-docker

docker ps 
docker ps -a >> all processes. 

docker run ubuntu >> downloads ubuntu
docker run -it ubuntu >> Starts ubuntu shell in interactive mode. 

Ubuntu: 

install - java.. 
apt list
apt update
apt install nano
apt remove nano
